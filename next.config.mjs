/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        optimizeCss: true
    },
    basePath: '/GAD-services',
    assetPrefix: '/GAD-services',
};

export default nextConfig;
